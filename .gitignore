# build artifacts
/projx-engine
/projx-engine.exe
*.exe
*.test

# per-project engine runtime state (store db, history, compiled context, jail shims)
.projx/
.runtime/
*.ctl
/data/
/data-*/
/dist/

# generated local agent integrations and caches
/.claude/
/.codegraph/
/.codex/
/.codex-remote-attachments/
/.mcp.json
/AGENTS.md
/CLAUDE.md
/.tmp*/

# local workspace file — the module builds via go.mod replace directives
go.work
go.work.sum
*.wasm
*.exe~
