---
description: Show the ProjX knowledge store for this project (low-token — bash does the work)
allowed-tools: Bash(projx-engine:*)
---
Current ProjX store contents:

!`projx-engine --root . store list`

Briefly summarize what's declared (conventions, gates, docs, code-map) if I asked a question; otherwise just surface the list.
