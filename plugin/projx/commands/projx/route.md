---
description: Show the ProjX routing decision (which model tier) for a task — no execution
allowed-tools: Bash(projx-engine:*)
---
ProjX routing decision for the task:

!`projx-engine --root . route "$ARGUMENTS"`

This is the decider's choice (class + why). It does not run anything.
